# 프로젝트: Outlier — 유튜브 다채널 유해댓글 관리 서비스

## 한 줄 요약
채널별 커스텀 LLM 기반으로, 여러 유튜브 채널의 댓글 유해성을 AI가 판별하고 관리자가 우선순위로 검토·조치할 수 있게 해주는 서비스. 광주 인공지능사관학교 기업연계 팀 프로젝트(팀명 Outlier, 멘토 정연주).

## 팀 구성
- 박승민 (팀장, PM·백엔드) — GitHub 레포 `tmdals5512/youtube-comment-moderator` 오너
- 김지황 (백엔드·DB, 댓글 수집·처리 파이프라인 담당) — 이 프롬프트를 작성한 사람, 지금부터 클로드코드로 개발 진행
- 박정호 (데이터 수집, YouTube Data API)
- 조민준 (PM·발표자료)
- 신효은 (Front-end, UI/UX)

## 지금까지 확정된 것 (기획 단계 완료)

### 판별 파이프라인 (4단계)
1. **1차 규칙기반** — 관리자가 채널별로 직접 등록한 금칙어/기준을 정규식으로 즉시 매칭 (F_R_109). COREPIN 호출 전 무료로 명백한 욕설을 걸러내 비용·속도 절감.
2. **2차 COREPIN API** — 외부 유해표현 탐지 API (M01~M11 카테고리, 이 중 M07 욕설·비속어/M08 성희롱/M09 위협·자해/M10 프롬프트 인젝션 4종은 기본 차단, 나머지는 채널별 토글). 호출당 5원, 응답속도 약 17ms.
3. **2.5차 업종별 커스텀 규칙기반 (F_R_127, 신설)** — COREPIN이 애매하다고 판단했거나, COREPIN이 신뢰도 높게 "정상"이라고 했더라도 텍스트 구조 이상 신호가 감지된 댓글을 대상으로, 업종(게임/아이돌팬덤/스포츠 등)별로 반복되는 애매·우회 표현 패턴을 LLM 호출 전에 규칙기반으로 우선 판별. LLM 호출 비용·응답시간 절감 목적.
4. **3차 커스텀 LLM** — 위 단계로도 판정 안 되는 댓글을 채널별 규칙·판단이력을 컨텍스트로 주입해 정밀판별 (F_R_111). 관리자 피드백이 계속 쌓여서 채널마다 점점 정확해지는 게 핵심 차별화 포인트.

### 중요 설계 원칙: "COREPIN 신뢰도 높음"만으로 자동확정하면 안 됨
COREPIN은 초성만으로 이루어진 표현(자모 나열)이나 낯선 은어·패드립을 학습 데이터 밖 입력으로 인식 못 해서, 오히려 확신을 갖고 "정상"으로 오판할 수 있음. 그래서 자동확정(F_R_113) 여부는 COREPIN 신뢰도 값 + **텍스트 구조 이상 신호(초성 비율 과다, 사전 미등재 어휘 등)**를 함께 봐야 함 — 이상 신호가 있으면 신뢰도가 높아도 자동확정 대상에서 제외하고 F_R_127 이후 단계로 넘김. 정확한 임계값/사전은 아직 미정 — 파일럿 데이터로 튜닝 예정.

### MVP 범위 (2가지 핵심 기능)
- **MVP①**: 관리자가 지정한 유해단어·기준을 걸러주는 기능 (1차 규칙기반의 자동화)
- **MVP②**: AI가 바로 확정하기 어려운 애매한 표현을, 근거·신뢰도·과거 유사사례와 함께 관리자에게 제시해 판단받는 기능 (COREPIN+커스텀 LLM)

### YouTube API 정책 제약 (공식 문서 대조 확인 완료)
- 댓글은 완전 삭제 불가, `comments.setModerationStatus(rejected)`로 비공개 처리(숨김)만 가능
- `banAuthor`는 채널/영상 단위 차단이지 플랫폼 전체 밴 아님
- 댓글 원문·작성자 정보는 수집 후 최대 30일만 보관, 이후엔 위험도 점수 등 파생 지표만 유지
- 서로 다른 채널(고객사) 데이터는 대시보드에서 결합·비교 노출 금지
- 채널 연동 해제/계약 종료 시 해당 채널 관련 API Data 전체 즉시 삭제
- 채널 연동 시 AI 자동 모더레이션에 대한 관리자 명시 동의 필요

### 유스케이스 (8개)
로그인(Google OAuth) → 채널 연동(YouTube) → 관리 기준 등록(유해단어) → 대시보드 조회 → 유해댓글 검토(4단계 판별) → 조치 실행(숨김·채널차단·유지) → 처리 이력/통계 조회 → 채널 연동 해제(데이터 전체 삭제)

전체 요구사항: 기능요구사항 F_R_101~127 (27개), 비기능요구사항 NF_R_101~112 (12개). 상세 내용은 팀 Notion "요구사항정의서" 페이지 참고 (필요하면 캡처해서 전달할 것).

## 지금까지 한 개발 작업

### DB — PostgreSQL + pgvector, Docker로 로컬에 이미 띄워둠
- 컨테이너: `docker run -d --name outlier-db -e POSTGRES_PASSWORD=<비번> -p 5432:5432 pgvector/pgvector:pg17`
- 접속 정보: host=localhost, port=5432, database=postgres, user=postgres
- pgvector 익스텐션 활성화 완료 (`CREATE EXTENSION vector;`) — 나중에 F_R_114 "과거 유사 사례" 검색(임베딩 유사도)에 쓸 예정
- **주의**: pgvector를 선택한 이유가 "과거 유사 사례" 의미 기반 검색 때문. MySQL이 아니라 PostgreSQL인 이유이기도 함.

### DB 스키마 — 뼈대만 잡아둔 상태, 컬럼은 확정 아님
아래 10개 테이블을 만들어뒀는데, **이건 어디까지나 초안(껍데기) 수준이고 정확한 컬럼 구성은 아직 안 정해짐.** 실제로 기능(댓글 수집기, COREPIN 연동, 판별 로직 등)을 만들면서 필요에 따라 컬럼을 자유롭게 추가/수정/삭제해도 됨 — 지금 스키마에 얽매이지 말 것.

- `workspaces` — 워크스페이스(팀/개인)
- `users` — 유저
- `workspace_members` — 워크스페이스-유저 연결(권한)
- `channels` — 연동된 유튜브 채널
- `channel_rules` — 채널별 유해단어/COREPIN 카테고리 설정 (F_R_108, F_R_126)
- `industry_rules` — 업종별 공용 규칙 (F_R_127)
- `comments` — 수집된 댓글 원문
- `risk_assessments` — 각 판별 단계별 위험도 결과
- `review_history` — 관리자 검토 이력 (F_R_116)
- `actions` — 조치 이력 (F_R_115)

### GitHub
레포: `tmdals5512/youtube-comment-moderator` (팀장 박승민 소유, push 권한 초대받음). 아직 로컬에 clone 안 한 상태일 수 있음 — 안 돼있으면 먼저 clone.

## 아직 안 정해진 것 / 나중에 파일럿 데이터로 튜닝할 것
- 텍스트 구조 이상 신호의 정확한 임계값(초성 비율 몇 %부터), 참조할 어휘 사전 종류
- 업종별 규칙기반(F_R_127)에 실제로 어떤 패드립·은어 단어를 등록할지 구체 목록
- COREPIN→3차 LLM 라우팅을 confidence 단일 임계값이 아니라 다중신호 가중치 스코어링으로 할지 여부
- NF_R_102(3차 응답속도)는 5초 이내 유지, 트래픽 비율 캡은 없음 — 파일럿 실측 후 별도 산정
- NF_R_104(관리자 검토 전환율)는 30% 이하 목표치, 파일럿 실측으로 검증·조정

## 지금 하려는 것
1. GitHub 레포를 로컬에 clone (아직 안 했으면)
2. Python/FastAPI 백엔드 프로젝트 초기 세팅
3. 위 DB(localhost:5432)에 연결하는 코드 작성
4. 그 다음: YouTube Data API 키 발급·OAuth 앱 등록 → 댓글 수집기 프로토타입(F_R_107) → COREPIN API 연동 테스트 → 1차 규칙기반 로직(F_R_109) 순서로 진행 예정

## 담당자 역할 참고
지금 이 프롬프트를 준 사람(김지황)은 백엔드·DB 담당. 데이터 수집(YouTube API)은 박정호, 프론트엔드는 신효은이 별도로 진행 중이니, 백엔드/DB/판별 파이프라인 범위에 집중해서 도와주면 됨.
