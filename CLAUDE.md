# Outlier — 유튜브 다채널 유해댓글 관리

광주 인공지능사관학교 기업연계 팀 프로젝트. 팀 Outlier, 멘토 정연주.
GitHub `tmdals5512/youtube-comment-moderator`, 작업 브랜치 `feat/moderation-pipeline`.

이 파일은 실제 코드 상태를 따른다. 기획 단계 문서(요구사항정의서 F_R_101~127)와
다른 부분은 여기가 맞다 — 측정해서 바꾼 것들이다.

## 팀
- 박승민 (팀장, PM·백엔드) — 레포 오너
- 김지황 (백엔드·DB·판별 파이프라인) — 이 레포에서 클로드코드로 개발하는 사람
- 박정호 (데이터 수집) · 조민준 (PM·발표) · 신효은 (프론트엔드)

## 작업 규칙 — 반드시
- **커밋·푸시는 시킬 때까지 하지 않는다.**
- **DB 에 쓰는 작업은 확인받고 한다.** 판별 저장(`--save`), 동의 플래그, 재판별 전부.
- **추측하지 말고 실데이터로 잰다.** "될 것 같다" 는 근거가 아니다. 돈이 들면 얼마인지 먼저 말한다.
- **프롬프트는 보수적으로.** 새 오판 하나로 고치지 않는다. `eval/오류기록.csv` 에 유형을
  붙여 적고, 같은 유형 3건이면 일반 원칙 한 줄. 그 댓글을 예시로 넣지 않는다.
  회귀 세트 점수는 ±2 흔들리므로 그 안의 변화는 변화가 아니다.
- **"정확도" 라는 말을 쓰지 않는다.** 회귀 세트는 개발자가 정답을 매긴 동작 검증이다.
  실제 정확도는 라벨링(`labeling/`, 4명 × 1,000건)이 나와야 말할 수 있다.
- **API 키를 채팅에 붙이지 않는다.** `.env` 를 cat 하지 않는다.
- **설명할 때는 그게 뭔지부터.** 용어를 아는 것으로 가정하지 않는다.
- **숫자를 말할 때 잰 것과 지어낸 것을 섞지 않는다.** 31.9초/건, 10분, 280자 —
  전부 근거 없이 말해서 지적받은 것들이다.

## 제품이 실제로 하는 것

```
구글 로그인 → 채널 연결(YouTube OAuth, 리프레시 토큰 저장) → AI 판별 동의
  ↓ 서버가 매시간 (watcher.py)
수집 (새 댓글만) → ① 관리자 등록어 → ② LLM 판별 → ③ 검토 큐
  ↓ 관리자가 하나씩
숨김 / 유지 / 채널차단 → 유튜브에 실제 반영 → 이력 · 복구 → 30일 파기
```

실채널로 끝까지 확인했다 (2026-09-15). 테스트 171개.

기획 단계의 4단계(규칙 → COREPIN → 업종 규칙 → LLM)는 **3단계(등록어 → LLM → 사람)** 가
됐다. COREPIN 은 무료 규칙 대비 1.4%p 차이에 건당 5원이라 뺐고, 업종별 규칙(F_R_127)은
텍스트 구조 신호가 변별력이 없어(악플 1.45% vs 정상 1.05%) 판별 단계로 쓰지 않는다.

## 측정해서 정한 것

| 정한 것 | 근거 |
|---|---|
| 자동 숨김 기본 꺼짐 | '욕설' 만 남겼을 때 방송 고지문·행동 지적·`보지마` 오독이 사람 안 거치고 가려짐 |
| 확신 없으면 ambiguous → 관리자 | 관리자가 안 본 채 남는 댓글이 없어야 한다는 게 제품 기준 |
| 채널별 기준을 프롬프트에 글로 안 쓴다 | 관리자 결정 30건→규칙 추출 63% 일치, 3건→한 문장 90%, 사람이 쓴 것 100%. 사례에서 규칙이 귀납되지 않는다 |
| 노출(좋아요) 순 정렬 안 함 | 관리자가 처리했다고 믿는데 댓글창에 100개가 남는 구조. 꼬리를 남기는 설계는 틀림 |
| 묶어서 한 번에 조치 안 함 | 안 본 것에 사람 도장을 찍는 장치 |
| LLM confidence 안 씀 | 맞을 때 0.98, 틀릴 때 0.96 |
| 프롬프트 1,024 토큰 이상 유지 | 캐싱 경계. 넘겨야 오히려 싸다 (168원→129원) |
| 채널 맥락은 프롬프트 맨 뒤 | 앞부분 캐시 재사용 |

## 프롬프트 (`app/services/llm.py`)

v4. 판단 순서 4단계(무엇이 있고 누구를 향하나 → 실제 공격인가 → 공격처럼 보이지만
아닌가 → 확정 못 하면 ambiguous) + 카테고리 11 + 예시 23. 이력·교훈은 파일 헤더에.

- v3 는 예시 43개, 회귀 세트 14건이 예시에 그대로 → 암기. v4 에서 0.
- 예시가 원칙을 이긴다. 원칙 안에서도 뒤의 센 문장이 앞을 이긴다.
- `[이 채널의 맥락]` 은 참고 정보. 원칙을 뒤집지 않는다.
- 회귀 세트 30±2/39. 남은 9건은 문화 지식(밈·관용) · 사람도 갈리는 경계 · 낡은 기대값.
  일반 원칙만으로 갈 수 있는 끝.
- 실데이터 300건 v3 대비 검토 큐 +8%p. 절반은 v3 가 채널 기준 때문에 놓치던 욕설.

## 데이터 현황 (2026-09-16)

| 채널 | 권한 | 동의 | 댓글 | 판정 |
|---|---|---|---|---|
| #1474 인공지능사관학교test | O | O | 16 | v3 (유일한 실연동 채널) |
| #4 진용진 | X | O | 4,568 | v3 + 채널 기준 (`저새끼` 등 통과된 상태) |
| #1 JTBC | X | O | 761 | v3 |
| #2 MBC경남 | X | X | 344 | 옛 것 |

샘플 채널의 동의 플래그는 개발자가 켠 것이다. 실제 채널 주인은 모른다.
권한(리프레시 토큰) 없는 채널은 조치가 유튜브에 안 간다 — 화면에 배너로 표시.

## 인프라
- PostgreSQL 17 + pgvector, Docker `outlier-db`. Docker Desktop 이 꺼지면 서버도 죽는다.
- LLM `gpt-5.6-luna`, `reasoning_effort=low`. 건당 0.17원(짧은 댓글)~0.31원(뉴스).
- 하루 LLM 상한 3,000건 (`LLM_DAILY_CAP`). 실행당 600건.
- 유튜브 쿼터 10,000/일. 감시 채널당 ~720.
- 판정은 같은 입력에도 약 8% 흔들린다 (재현성 92%).

## 도구
```
python -m scripts.check_prompt [--channel N]   회귀 세트 채점. 7원
python -m scripts.show_prompt [N]              LLM 이 받는 프롬프트 그대로
python -m scripts.run_pipeline --channel N [--save]
python -m scripts.collect_channel @핸들 5 60
python -m scripts.consent --channel N --agree
python run.py                                  서버 (DB 상태 확인 포함)
```
`eval/` 과 `labeling/` 은 gitignore. 실제 댓글이 들어 있다. 회귀 세트·오류 기록은 팀 내부 전달.

## 정하지 않은 것 / 미룬 것
- **위험도 표** `review.py SEVERITY` — 개발자가 정함. 큐의 65~80% 가 '모욕=보통' 한 칸.
  LLM 이 직접 매기려면 등급 정의(긴급이 뭔지)를 팀이 써야 한다. 초안 있음, 승인 대기.
- **채널별 기준 만드는 방법** — 위 실험 결과. 3클릭→문장 90% 가 가장 나았으나 뽑힌 문장
  반이 범위가 틀려 보류.
- **권한(owner/admin/member)** — 저장은 되나 확인 안 함. 굳이 필요 없다고 결정.
- **회귀 세트 기대값 4개** — 山(애매→통과?), 凸 계열(유해→애매?), 여자들때매(통과→애매?).
  지황님이 정할 것.
- 작성자별 묶기 · 영상별 필터 · 배포 — 미룸.
- 라벨링 배포 — 시트는 있음. 아무도 안 함. 이게 "정확도" 를 말할 유일한 길.

## 유튜브 API 정책 (변함없음)
- 댓글 삭제 불가. `setModerationStatus(rejected)` 로 비공개만.
- `banAuthor` 는 채널 단위.
- 원문·작성자 30일 보관 후 파기 (서버가 매일 자동).
- 채널 간 데이터 결합·비교 노출 금지 (`require_channel` 이 유일한 관문, 남의 것은 404).
- 연동 해제 시 전체 삭제.
- AI 판별에 관리자 명시 동의 필요.
